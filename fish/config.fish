set fish_greeting ""

# alias list directory contents
alias ll "ls -l"
alias la "ls -la"
# alias to cd to parent directory
alias .. "cd .."

# alias to tmux attach -t
alias ta "tmux attach -t"

set -g fish_color_command cyan